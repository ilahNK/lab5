import socket
import sys
import json


clientSocket = socket.socket()
port = 8080
clientSocket.connect(('192.168.1.6', port))

file = open('5.4.txt', 'r')
file1 = file.read()
print (file1)
sendData = json.dumps(file1)

try:
    clientSocket.connect((' ', port))
except socket.error as e:
    print(str(e))

while True:
    Input = input('Left Message:')
    clientSocket.send(str.encode(Input))
    Response = clientSocket.recv(1024)
    print(Response.decode('utf-8'))


clientSocket.sendall(bytes(sendData,encoding="utf-8"))
sys.exit()
clientSocket.close()

